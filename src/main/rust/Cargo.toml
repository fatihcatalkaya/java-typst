[package]
name = "java-typst"
version = "2.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
typst-as-lib = { version = "0.15", default-features = false, features = ["typst-kit-fonts", "typst-kit-embed-fonts"] }
typst-pdf = "0.14"
tar = "0.4"
flate2 = "1.0"
