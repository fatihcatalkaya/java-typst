<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.fatihcatalkaya</groupId>
    <artifactId>java-typst</artifactId>
    <version>2.2.0</version>
    <packaging>jar</packaging>
    <licenses>
        <license>
            <name>Apache License 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <name>${project.groupId}:${project.artifactId}</name>
    <description>A library that allows to render Typst markup language to pdf files</description>
    <url>https://github.com/fatihcatalkaya/java-typst</url>

    <developers>
        <developer>
            <name>Fatih Çatalkaya</name>
            <email>fatih.catalkaya@yahoo.de</email>
            <url>https://github.com/fatihcatalkaya</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/fatihcatalkaya/java-typst.git</connection>
        <developerConnection>scm:git:git://github.com/fatihcatalkaya/java-typst.git</developerConnection>
        <url>https://github.com/fatihcatalkaya/java-typst/tree/master</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <chicory.version>1.7.5</chicory.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>runtime</artifactId>
            <version>${chicory.version}</version>
        </dependency>
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>wasm</artifactId>
            <version>${chicory.version}</version>
        </dependency>
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>wasi</artifactId>
            <version>${chicory.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <profiles>
        <profile>
            <id>rust</id>
            <build>
                <plugins>
                    <!-- 1. Invoke cargo; .cargo/config.toml sets target = wasm32-wasip1 -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.4.1</version>
                        <executions>
                            <execution>
                                <id>cargo-build-wasm</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>cargo</executable>
                                    <workingDirectory>${project.basedir}/src/main/rust</workingDirectory>
                                    <arguments>
                                        <argument>build</argument>
                                        <argument>--release</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>wasm-opt</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>wasm-opt</executable>
                                    <arguments>
                                        <argument>-Oz</argument>
                                        <argument>-o</argument>
                                        <argument>
                                            ${project.basedir}/src/main/rust/target/wasm32-wasip1/release/java_typst_opt.wasm
                                        </argument>
                                        <argument>
                                            ${project.basedir}/src/main/rust/target/wasm32-wasip1/release/java_typst.wasm
                                        </argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- 2. Copy the .wasm into target/classes so it lands on the classpath -->
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copy-wasm-to-classes</id>
                                <phase>process-sources</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <mkdir dir="${project.build.outputDirectory}/io/github/fatihcatalkaya/javatypst"/>
                                        <copy
                                                file="${project.basedir}/src/main/rust/target/wasm32-wasip1/release/java_typst_opt.wasm"
                                                tofile="${project.build.outputDirectory}/io/github/fatihcatalkaya/javatypst/java_typst.wasm"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.dylibso.chicory</groupId>
                        <artifactId>chicory-compiler-maven-plugin</artifactId>
                        <version>${chicory.version}</version>
                        <executions>
                            <execution>
                                <id>compiler-gen</id>
                                <goals>
                                    <goal>compile</goal>
                                </goals>
                                <configuration>
                                    <wasmFile>
                                        ${project.basedir}/src/main/rust/target/wasm32-wasip1/release/java_typst_opt.wasm
                                    </wasmFile>
                                    <name>io.github.fatihcatalkaya.javatypst.JavaTypstModule</name>
                                    <moduleInterface>io.github.fatihcatalkaya.javatypst.JavaTypstInterface
                                    </moduleInterface>
                                    <interpreterFallback>WARN</interpreterFallback>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.4.0</version>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.43.0</version>
                <configuration>
                    <java>
                        <palantirJavaFormat>
                            <version>2.50.0</version>
                        </palantirJavaFormat>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                The main artifact is a thin jar so that consumers resolve chicory through normal
                transitive dependency resolution. Overwriting it with the shaded assembly used to
                ship chicory's classes twice - once unpacked in this jar, once via the declared
                dependencies - which breaks any build that dexes or merges the classpath (Android)
                and silently pins consumers to our chicory version everywhere else.

                The all-in-one jar is still published, under the jar-with-dependencies classifier.
            -->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
                <executions>
                    <execution>
                        <id>fat-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <descriptorRefs>
                                <descriptorRef>jar-with-dependencies</descriptorRef>
                            </descriptorRefs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <sourcepath>${project.build.sourceDirectory}</sourcepath>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>3.2.3</version>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <keyname>44D7F1B70392915130F652E10B824C8E733B1743</keyname>
                    <gpgArguments>
                        <argument>--pinentry-mode</argument>
                        <argument>loopback</argument>
                    </gpgArguments>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.7.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
